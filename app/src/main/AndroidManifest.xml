<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Very Important to get the slider working -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:enableOnBackInvokedCallback="true"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Material3ComposeTheme"
        tools:targetApi="31">
        <activity
            android:name=".BottomNav"
            android:exported="false"
            android:label="@string/title_activity_bottom_nav"
            android:theme="@style/Material3ComposeTheme" />
        <activity
            android:name=".CashPayment"
            android:exported="false"
            android:label="@string/title_activity_cash_payment"
            android:theme="@style/Material3ComposeTheme" />
        <activity
            android:name=".IconGrid"
            android:exported="false"
            android:label="@string/title_activity_icon_grid"
            android:theme="@style/Material3ComposeTheme" />
        <activity
            android:name=".Icons"
            android:exported="false"
            android:label="@string/title_activity_icons"
            android:theme="@style/Material3ComposeTheme" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Material3ComposeTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>